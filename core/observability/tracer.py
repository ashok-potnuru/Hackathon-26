"""
Traces a full pipeline run per issue, linking all stage logs into a single trace.
Implement span creation, context propagation, and trace export here.
Used for debugging end-to-end pipeline failures across distributed stages.
"""
