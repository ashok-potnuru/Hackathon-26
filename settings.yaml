llm: claude
issue_tracker: zoho          # zoho | zoho_tasks | jira | linear
version_control: github      # github | gitlab | azure_devops
notification: teams          # teams | slack | discord
cloud: aws                   # aws | gcp | azure
vector_store: chromadb       # chromadb | pinecone
