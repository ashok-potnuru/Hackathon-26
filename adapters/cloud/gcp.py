"""
GCP implementation of CloudBase using GCS (storage), Pub/Sub (queue), and Secret Manager (secrets).
Implement all base methods using the google-cloud-* client libraries.
To activate: set cloud: gcp in settings.yaml.
"""
